# Ensure exact versions are installed
save-exact=true

# Use package-lock.json for deterministic installs
package-lock=true

# Engine strict mode - fail if Node/npm version doesn't match
engine-strict=true

# Prevent auto-updating packages
save-prefix=

# Audit level
audit-level=moderate

